
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: agentconfigs.hubofhubs.open-cluster-management.io
spec:
  group: hubofhubs.open-cluster-management.io
  names:
    kind: AgentConfig
    listKind: AgentConfigList
    plural: agentconfigs
    singular: agentconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentConfig is the Schema for the agentConfigs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AgentConfigSpec defines the desired state of AgentConfig
            properties:
              components:
                description: AgentComponentsConfig defines settings for leaf hub components
                properties:
                  core:
                    description: AgentCoreConfig defines settings for leaf hub core
                      controllers
                    properties:
                      leafHub:
                        description: LeafHubConfig defines settings for core controllers
                          in leaf hub cluster
                        properties:
                          specSync:
                            description: LeafHubSpecSyncConfig defines settings for
                              leafhub-spec-sync
                            properties:
                              enforceHoHRbac:
                                type: boolean
                              kubeClientPoolSIze:
                                format: int64
                                type: integer
                            type: object
                          statusSync:
                            description: LeafHubStatusSyncConfig defines settings
                              for leafhub-status-sync
                            properties:
                              deltaSentCountSwitchFactor:
                                format: int64
                                type: integer
                              msgCompressType:
                                description: MsgCompressType specifies the type of
                                  message compress should do before sending the information
                                  to transport
                                enum:
                                - gzip
                                - no-op
                                type: string
                              msgSizeLimit:
                                format: int64
                                type: integer
                              syncIntervalConfig:
                                description: LeafHubStatusSyncIntervalSettings defines
                                  snyc interval settings for leahub-status-sync
                                properties:
                                  controlInfo:
                                    format: int64
                                    type: integer
                                  managedClusters:
                                    format: int64
                                    type: integer
                                  policies:
                                    format: int64
                                    type: integer
                                type: object
                            type: object
                        type: object
                    type: object
                  transport:
                    description: AgentTransportConfig defines settings for transport
                      layer
                    properties:
                      provider:
                        description: TransportProvider specifies the provider type
                          of transport layer
                        enum:
                        - kafka
                        - sync-service
                        type: string
                      syncService:
                        description: Kafka       *KafkaConfig       `json:"kafka,omitempty"`
                        properties:
                          pollingInterval:
                            format: int64
                            type: integer
                          version:
                            type: string
                        type: object
                    type: object
                type: object
              global:
                description: AgentGlobalConfig defines common settings for leaf hub
                properties:
                  heartbeatInterval:
                    description: AgentHeartbeatIntervalConfig defines heartbeat interval
                      in seconds leaf hub
                    properties:
                      leafHub:
                        format: int64
                        type: integer
                    type: object
                type: object
            type: object
          status:
            description: AgentConfigStatus defines the observed state of AgentConfig
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
